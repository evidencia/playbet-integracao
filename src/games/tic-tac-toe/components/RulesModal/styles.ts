import styled from 'styled-components';

export const RuleTitle = styled.p`
  font-size: 26px;
  font-weight: bold;
  margin-bottom: 6px;
`;

export const BoldRule = styled.p`
  font-size: 22px;
  font-weight: bold;
  margin-bottom: 6px;
  margin-top: 16px;
`;

export const DrawRule = styled.p`
  margin-top: 8px;
  font-weight: bold;
`;
